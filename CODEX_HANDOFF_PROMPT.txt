You are working in my GitHub repository. I have provided a scaffold package for a public Codex plugin/skill called Sports Prediction Codex Skill.

Please merge the scaffold into the current repo, preserving any existing user code or useful files. Do not force push. Do not delete user content. Confirm repository links point to `898038186sh-debug/Edison-sports-prediction-codex`.

Main goals:
1. Make this repo a public-ready Codex repo marketplace with `.agents/plugins/marketplace.json`.
2. Keep the plugin at `plugins/sports-prediction-codex-plugin/` with `.codex-plugin/plugin.json` and `skills/sports-prediction/SKILL.md`.
3. Keep all disclaimers and responsible-use language.
4. Never market this as a winning betting system or a guaranteed prediction tool.
5. Validate JSON, scan for secrets, and create a branch + PR if GitHub CLI is available.

After editing, run:
python -m json.tool .agents/plugins/marketplace.json
python -m json.tool plugins/sports-prediction-codex-plugin/.codex-plugin/plugin.json
python scripts/validate_repo.py || true

git status

If gh is available and authenticated, suggest or run:
gh repo edit --description "A transparent Codex skill/plugin for structured sports match prediction reports, team form analysis, odds context, and uncertainty-aware forecasts." --enable-issues --enable-discussions --add-topic codex --add-topic codex-plugin --add-topic agent-skills --add-topic skill-md --add-topic sports-analytics --add-topic sports-prediction --add-topic match-prediction --add-topic football --add-topic soccer --add-topic nba --add-topic mlb --add-topic odds-analysis --add-topic prediction-markets --add-topic ai-agents

Create a branch named `release/public-codex-plugin-v0.1.0`, commit with message `Prepare public Sports Prediction Codex plugin`, push it, and open a PR if possible. If you cannot do any step, tell me exactly what I need to do manually.
