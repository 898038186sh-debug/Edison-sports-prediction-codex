name: Publish Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create v0.1.3 release if missing
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view v0.1.3 >/dev/null 2>&1; then
            echo "Release v0.1.3 already exists."
            exit 0
          fi
          gh release create v0.1.3 \
            --target "$GITHUB_SHA" \
            --title "v0.1.3 - Update public repository links" \
            --notes-file RELEASE_NOTES_v0.1.3.md
